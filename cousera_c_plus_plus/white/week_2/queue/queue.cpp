#include<vector>
#include<string>
#include<iostream>

using namespace std;

int main () {
    vector<bool> people_in_queue = {};
    int N = 0; // количество операций
    cin >> N;
    for (int i = 0; i < N; ++i) {
        string A;
        int B;
        cin >> A >> B;
    }
    return 0;
}